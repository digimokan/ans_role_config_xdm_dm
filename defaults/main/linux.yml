# path to main Linux Xresources file for XDM
linux_xdm_xresources_file_path: "/etc/X11/xdm/Xresources"

# path to Linux XSetup utilities-startup file for XDM (starts on console 0)
linux_xdm_xsetup_file_path: "/etc/X11/xdm/Xsetup_0"

# path to Linux buttons script for XDM
linux_xdm_buttons_file_path: "/etc/X11/xdm/buttons.sh"

# Linux system-shutdown command (for buttons)
linux_shutdown_cmd: '/usr/bin/poweroff'

# Linux system-restart command (for buttons)
linux_restart_cmd: '/usr/bin/reboot'

# Description of available virtual consoles on Linux
linux_virt_consoles_msg: 'Virtual Consoles: 1 (Login/Xorg, Ctl-Alt-F1) to 6 (Ctl-Alt-F6)'

